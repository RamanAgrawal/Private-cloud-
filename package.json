{
  "name": "private-cloud",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend && node scripts/deploy.js",
    "start": "npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
